# NixOS / Nix Dev Environment

If you use NixOS or have the Nix package manager installed, a dev shell with JDK 25 is provided via flake:

```bash
nix develop   # enter dev shell
# or with direnv:
direnv allow  # auto-activates when entering the directory
```

See [README.md](README.md) for usage instructions.
